/**
 * @internal
 * Internal helper to create the proxy object
 * It lifes as standalone function because we dont need to carrie it in memory for every ActionHandler instance
 * @param subjects
 * @param transforms
 */
import { Subject } from 'rxjs';
import { ValuesOf, RxActions } from './types';
import { ErrorHandler } from '@angular/core';
export declare function actionProxyHandler<T extends object, U>(subjects: {
    [K in keyof T]: Subject<ValuesOf<T>>;
}, transforms?: U, errorHandler?: ErrorHandler): ProxyHandler<RxActions<T, U>>;
//# sourceMappingURL=proxy.d.ts.map