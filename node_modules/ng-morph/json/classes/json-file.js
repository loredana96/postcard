"use strict";
/**
 * @license
 * Copyright Google Inc. All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.JSONFile = void 0;
const json_file_content_1 = require("./json-file-content");
class JSONFile extends json_file_content_1.JSONFileContent {
    constructor(host, path) {
        const buffer = host.read(path);
        if (buffer) {
            super(buffer.toString());
        }
        else {
            throw new Error(`Could not read '${path}'.`);
        }
        this.host = host;
        this.path = path;
    }
    modify(jsonPath, value, insertInOrder) {
        super.modify(jsonPath, value, insertInOrder);
        this.host.overwrite(this.path, this.content);
    }
    jsonAst() {
        try {
            return super.jsonAst();
        }
        catch (e) {
            throw new Error(`[${this.path}] ${e.message}`);
        }
    }
}
exports.JSONFile = JSONFile;
//# sourceMappingURL=json-file.js.map